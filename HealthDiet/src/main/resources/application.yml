server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/food?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: maxu20040310
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto  # 添加这一行：全局配置主键自增
      # 可选配置
      table-prefix: ''  # 表前缀，如果有需要可以配置
      logic-delete-field: deleted  # 逻辑删除字段

ai:
  api-key: sk-euyokciyjcklncfknuakusrmlhivhfqqzwexgbxbraailpjd
  url: https://api.siliconflow.cn/v1/chat/completions